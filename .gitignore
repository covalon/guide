# Obsidian: per-device window layout and caches, and personal settings (theme, accent colour, which
# snippets are turned on: the site uses every snippet anyway)
.obsidian/appearance.json
.obsidian/workspace.json
.obsidian/workspace-mobile.json
.obsidian/cache
.trash/

# macOS / Windows clutter
.DS_Store
Thumbs.db

# the site build: Python caches, and a site built into the vault by hand
# (build-local.sh builds outside the vault, in ~/.covalon-site)
__pycache__/
*.pyc
/public/
.site-build/_preview*.tgz
